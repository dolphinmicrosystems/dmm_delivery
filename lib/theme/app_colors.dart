import 'package:flutter/material.dart';

/// Design tokens lifted from prototypeAssets/styles.css.
class AppColors {
  AppColors._();

  static const brand = Color(0xFF1A56DB);
  static const brandDark = Color(0xFF153F9E);
  static const brandSoft = Color(0xFFEAF0FB);

  static const surfaceMuted = Color(0xFFF5F7FA);
  static const ink = Color(0xFF0B1220);
  static const inkMuted = Color(0xFF64748B);
  static const hairline = Color(0xFFE5E9F0);

  static const success = Color(0xFF1FA971);
  static const warning = Color(0xFFE4A83A);
}
